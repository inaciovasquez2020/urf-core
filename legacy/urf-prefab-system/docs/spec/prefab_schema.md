# Prefab Schema

Prefab definition file format (JSON)

{
  "name": "prefab_name",
  "inputs": "input type description",
  "constraints": "admissibility conditions"
}

Fields

name
unique prefab identifier

inputs
schema describing acceptable inputs

constraints
admissibility conditions required before normalization

# DraG0n Representation-Invariant Minimal Augmentation Theorem: Unconditional Corollary

## Status

CONDITIONAL ON `DRAGON_FINITE_QUOTIENT_ADMISSIBILITY_LEMMA`

## Corollary

Assume the Finite Quotient Admissibility Lemma.

Then for every diagnostic object `D`,
\[
\widehat{\mathrm{DraG0n}}^{(k)}(D)=1
\Longrightarrow
\exists\ \preceq\text{-minimal admissible augmentation }(Z,\pi)
\]
such that
\[
\widehat{\mathcal C}(D)-\widehat{\mathcal C}(D\mid Z,\pi)\ge\tau,
\]
and this minimal augmentation class is invariant under diagnostic equivalence `D\sim D'`.

name: lean

on:
  push:
    paths-ignore:
      - zloop/**
  pull_request:
    paths-ignore:
      - zloop/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: leanprover/lean-action@v1
        with:
          build: true
